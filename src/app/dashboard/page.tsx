'use client';

// This page has been removed and now redirects to /dashboard/create.
// The layout handles the redirection logic.
// This file can be deleted in the future if no longer needed for routing.
export default function DashboardPage() {
  return null;
}
